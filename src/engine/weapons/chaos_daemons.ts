/**
 * Chaos Daemons weapon overrides.
 *
 * Currently no archetype-driven weapon substitutions exist for Chaos Daemons.
 * Add entries here as they are identified — same pattern as engine/weapons/csm.ts.
 *
 * Example pattern:
 *   'Archetype Name': {
 *     'Original Weapon': { name: 'Replacement Weapon', addAbility: '...' },
 *   }
 */

// Placeholder — no overrides yet
export const CD_VEHICLE_WEAPON_OVERRIDES: Record<string, Record<string, { name?: string; addAbility?: string }>> = {};
