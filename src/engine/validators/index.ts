/**
 * Barrel for faction-specific validators.
 * Sub-validators import ValidationItem directly from '../validators' (type-only, no circular risk).
 */
export { validateSpaceMarines } from './space_marines';
